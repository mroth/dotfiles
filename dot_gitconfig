[user]
	name = Matthew Rothenberg
	email = mroth@mroth.xyz
	signingkey = /Users/mroth/.ssh/id_ed25519.pub

[github]
	user = mroth

[gpg]
	format = ssh

[core]
	excludesfile = ~/.gitignore

[init]
	defaultBranch = main

[color]
	ui = true

[diff]
	tool = opendiff

[merge]
	tool = opendiff

[rebase]
	autosquash = true
	autostash = true

[push]
	default = simple
	autoSetupRemote = true

[pull]
	ff = only

[core]
	quotepath = false

[credential]
	helper = osxkeychain

[http]
    cookiefile = ~/.gitcookies

[alias]
	amend = commit --amend -C HEAD
	smash = rebase --autosquash --committer-date-is-author-date
	log = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
	dshow = -c diff.external=difft show --ext-diff
    ddiff = -c diff.external=difft diff

# GitHub for Mac insists on adding these whenever it launches, so let's check it
# in a way that we at least knows where it comes from.
[filter "hawser"]
	clean = git hawser clean %f
	smudge = git hawser smudge %f
	required = true

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

# git-sigil settings
# wip: https://github.com/mroth/git-sigil
# (previously: https://github.com/mroth/git-prompt-useremail)
[sigil.email "mroth@users.noreply.github.com"]
    symbol = 🫥
[sigil.email "mroth@mroth.xyz"]
    symbol = 🫆
[sigil.email "mroth@rocket.ws"]
    symbol = 🚀
[sigil.email "mroth@protocol.ai"]
    symbol = 🪐
[sigil.email "mroth@flickr.com"]
    symbol = 🌈🐼
[sigil.email "mroth@bitly.com"]
    symbol = 🐡
[sigil.email "mroth@dojo.com"]
    symbol = 🐲
[sigil.email "@stripe.com"]
    symbol = 🏦
[sigil.email "@gmail.com"]
    symbol = 📧


# include org specific directives based on location.
# https://git-scm.com/docs/git-config#_conditional_includes

[includeIf "gitdir:~/src/protocol/"]
	path = ~/src/protocol/.gitconfig.inc

[includeIf "gitdir:~/src/dojo/"]
    path = ~/src/dojo/.gitconfig.inc
